<% if @chat.title_previously_changed? %>
  <%= turbo_stream.update "chat-title", @chat.title %>  
<% end %>

<%= turbo_stream.update :new_message_form do %>
  <%= render "messages/form", chat: @chat, message: Message.new %>
<% end %>