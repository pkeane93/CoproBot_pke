<section class="bg-white lg:grid lg:h-screen lg:place-content-center">
  <div class="mx-auto w-screen max-w-7xl px-4 py-16 sm:px-6 sm:py-24 md:grid md:grid-cols-2 md:items-center md:gap-4 lg:px-8 lg:py-32">
    <div class="max-w-prose text-left">
      <h1 class="text-4xl font-bold text-gray-900 sm:text-5xl">
        Legal, Accounting, Maintenance & GDPR — All in One Chatbot
        <strong class="text-indigo-600"> Get instant, reliable</strong>
        answers for all your co-ownership questions
      </h1>

      <p class="mt-4 text-base text-pretty text-gray-700 sm:text-lg/relaxed">
       Instant answers. Stronger client confidence. Faster decisions.
      </p>

      <div class="mt-4 flex gap-4 sm:mt-6">
        <a href="#cards-container" class="inline-block rounded border border-indigo-600 bg-indigo-600 px-5 py-3 font-medium text-white shadow-sm transition-colors hover:bg-indigo-700" href="#">
          Get Started
        </a>

        <%# <a class="inline-block rounded border border-gray-200 px-5 py-3 font-medium text-gray-700 shadow-sm transition-colors hover:bg-gray-50 hover:text-gray-900" href="#">
          Learn More
        </a> %>
      </div>
    </div>
    <%= image_tag "Screenshot 2025-11-21 121809.jpg", alt: "Product demo illustration" %>
  </div>
</section>

<hr class="mx-20">

<div id="cards-container" class="w-[1200px] container mx-auto">
  <h1 class="text-5xl font-bold mb-4 mt-20">Start Your Conversation With the Right Co-Ownership Expert</h1>
  <p>Tap a category to access tailored co-ownership support.</p>

<div class="cards grid grid-cols-4 gap-4 mt-15">
  <%# CO-OWNERSHIP old path: section_chats_path(section.id)%>
  <%@sections.each do |section| %>
    <%# The link_to needs to be linked the the chats/id show page. probably have to mod the controller%>
    <%= link_to section_chats_path(section), data: { turbo_method: :post } do%>
    <div class="card bg-base-100 w-66 h-100 shadow-sm group block mb-20 border border-gray-200">
      <div class="card-body">
        <h2 class="card-title"><%= section.name %></h2>
        <p> <%= section.description%></p>
      </div>
      <hr class="mx-10 mb-5 border-gray-300">
      <figure>
        <%= image_tag section.url, alt: "Product demo illustration" %>
      </figure>
    </div>
    <%end%>
  <%end%>
</div>

</div>
